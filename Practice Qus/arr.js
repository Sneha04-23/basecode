//
let fruits=["apple","orange"]
fruits.push("mango","banana") // it is used to add the element in to the array at the end
console.log(fruits)
fruits.pop()// it is used to remove the last element from the array
console.log(fruits)
fruits.shift()// it is used to remove the 1st element from the array
console.log(fruits)
fruits.unshift("banana")// it is used to add the element to the array at the frond
console.log(fruits)
let arr=["red","black","white"]
console.log(arr.indexOf("red"))// it is use to find the place of the element
console.log(arr.includes("red"))// it is use to find the element present in the string or not
